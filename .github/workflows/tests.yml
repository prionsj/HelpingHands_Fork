name: CI

on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    env:
      CI: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18.x' 

      - name: Install dependencies
        run: |
          if [[ -f "yarn.lock" ]]; then
            yarn install
          else
            npm install --force
          fi

      - name: Run Unit tests
        run: npm run test

      - name: Build frontend_helpinghandsapp
        working-directory: Frontend/helpinghandsapp
        run: |
          npm install --force
          npm run build

      - name: Run Cypress UI Tests
        working-directory: Frontend/helpinghandsapp
        run: npx cypress run --component s

import React from 'react';

// Komponente für die Fehlerseite, die angezeigt wird, wenn eine nicht vorhandene Seite aufgerufen wird
const Error = () => {
    return (
        <div>
            <p>Error: Diese Seite existiert nicht!</p>
        </div>
    );
}

export default Error;